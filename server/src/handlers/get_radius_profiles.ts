import { type RadiusProfile } from '../schema';

export async function getRadiusProfiles(): Promise<RadiusProfile[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all available Radius profiles from the database.
    // Should return profiles with their configuration details (speeds, quotas, pricing).
    return [];
}